<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>

    </style>
    <style name="styleA">
        <item name="android:textSize">@dimen/txt_kow_easy</item>
        <item name="android:fontFamily">@font/aclonica_regular</item>
    </style>
    <style name="TextLabel" parent="Widget.Design.TextInputLayout">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/gray</item>
        <item name="android:textSize">16sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/red_test365</item>
        <item name="colorControlNormal">@color/app_bar</item>
        <item name="colorControlActivated">@color/gray</item>
    </style>
    <style name="ToolbarStyle" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

<!--    <style name="text_label_m" parent="TextAppearance.Design.Hint">-->
<!--        <item name="android:textSize">@dimen/text_12sp</item>-->
<!--    </style>-->

    <style name="edit_text_basic_material" parent="Widget.Design.TextInputLayout">
        <item name="errorEnabled">true</item>
        <item name="hintTextAppearance">@style/text_label</item>
        <item name="android:theme">@style/AppThemeSecond</item>
    </style>
    <style name="text_label" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/colorAccent</item>
    </style>
    <style name="AppThemeSecond" parent="AppTheme">
        <item name="colorControlActivated">@color/colorAccent</item>
    </style>

    <style name="TabLayoutStyleElevator" parent="Base.Widget.Design.TabLayout">
        <item name="android:textSize">@dimen/text_15sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>
